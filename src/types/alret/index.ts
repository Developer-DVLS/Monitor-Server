export interface AlertCardPayload {
  title: string;
  monitor: string;
  cause: string;
  length: string;
  checked_url: string;
  response: string;
  incident_url: string;
  monitor_url: string;
  incident_key: string;
}
